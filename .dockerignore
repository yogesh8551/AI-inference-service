# Ignore everything by default to keep the build context minimal (~0 bytes)
*

# Allow the build directory and all its contents
!build/
!build/**

# Allow entrypoint script and Dockerfile
!entrypoint.sh
!Dockerfile
